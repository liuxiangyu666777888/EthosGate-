# EthosGate · 善治 — 设计文档

> **项目定位**：面向大语言模型部署决策的多方参与伦理评估工具，将负责任创新四维框架（预测-反思-包容-响应）从规范性论证转化为可操作的评估流程。

---

## 一、项目动机与理论来源

### 1.1 问题陈述

当前LLM部署前评估以技术基准为主（MMLU、HumanEval、TruthfulQA等），缺少系统性的伦理评估工具。红队测试多为企业内部非公开活动，评估维度、方法和结论缺乏可复核性。结果：上线前评估是"封闭的工程判断"而非"可审查的公共实践"。

### 1.2 理论来源

基于论文《从价值对齐到负责任创新》的核心论断：价值对齐技术（RLHF等）是"智能向善"的必要条件而非充分条件；负责任创新的四维框架（Stilgoe, Owen & Macnaghten, 2013）可以弥补纯技术对齐在治理合法性上的不足。

本工具将四维框架转化为可操作的四阶段评估工作流。

### 1.3 工具定位

| 不是 | 而是 |
|------|------|
| 又一个AI安全评测基准 | 评估**过程**的工具，结构化审议而非替代判断 |
| 全自动伦理打分机 | LLM辅助 + 人类主导的决策支持系统 |
| 面向研究者的研究工具 | 面向**工程团队、产品经理、合规人员、外部审查者**的协作工具 |
| 一次性上线前检查表 | 支持部署后持续监控与迭代复审 |

---

## 二、系统架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端 (React / Next.js)                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │ 项目向导  │ │ 预测模块  │ │ 反思模块  │ │ 包容模块  │   │
│  │          │ │          │ │          │ │          │   │
│  │ 响应模块  │ │ 报告预览  │ │ 历史记录  │ │ 模板管理  │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘   │
│                                                         │
│  可视化组件: ECharts (风险热力图, 利益相关者矩阵, 时间线) │
└──────────────────────┬──────────────────────────────────┘
                       │ REST API
┌──────────────────────┴──────────────────────────────────┐
│                  后端 (Python / FastAPI)                  │
│  ┌──────────────┐ ┌──────────────┐ ┌────────────────┐  │
│  │ 评估引擎     │ │ 报告生成器   │ │ 项目存储层     │  │
│  │ · 规则评分   │ │ · LaTeX→PDF  │ │ · SQLite/      │  │
│  │ · LLM查询    │ │ · HTML模板   │ │   PostgreSQL   │  │
│  │ · 风险计算   │ │ · Markdown   │ │ · 模板库(JSON) │  │
│  └──────────────┘ └──────────────┘ └────────────────┘  │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────┴──────────────────────────────────┐
│                  LLM集成层 (可选, 可配置)                 │
│  · 场景生成 · 盲点分析 · 机制推荐 · 报告叙述生成         │
│  · 支持多后端: OpenAI / Anthropic / 本地模型             │
└─────────────────────────────────────────────────────────┘
```

### 2.2 技术选型

| 层 | 技术 | 理由 |
|----|------|------|
| 前端框架 | React + TypeScript | 组件化适合多模块仪表盘 |
| 可视化 | ECharts | 中文场景最成熟，支持热力图/矩阵/桑基图 |
| 后端 | Python FastAPI | 轻量、异步、与ML生态兼容 |
| 报告生成 | WeasyPrint + Jinja2 | HTML→PDF，中文支持好 |
| 数据存储 | SQLite (单机) / PostgreSQL (协作) | 渐进复杂度 |
| LLM集成 | LiteLLM | 统一多供应商接口 |

---

## 三、核心模块设计

### 3.1 项目设置向导 (Project Setup Wizard)

**入口**：新建项目时触发，结构化收集部署上下文。

**输入字段**：
- 模型基本信息：名称、版本、基础模型、参数量级
- 部署场景：目标领域（教育/医疗/金融/政务/通用等）
- 目标用户：用户群体描述、语言/地域范围
- 交互方式：API/聊天界面/嵌入式/Agent
- 团队信息：开发团队构成、是否有合规/伦理角色

**输出**：项目上下文配置文件，决定后续模块的问卷分支和风险权重。

---

### 3.2 预测模块 (Anticipation)

**对应论文 4.1 节**：从被动修复到前瞻治理。

#### 3.2.1 结构化风险问卷

按风险维度组织，每个维度3-5个核心问题（5级李克特量表 + 自由文本补充）：

| 风险维度 | 示例问题 |
|----------|---------|
| 偏见与公平 | "训练数据是否包含对特定群体的历史性偏见？""模型在方言/非标准语言上的表现是否有评估？" |
| 误用风险 | "模型是否可能被用于生成虚假信息？""是否有对抗性提示注入的防护？" |
| 隐私 | "训练数据中是否包含PII？""模型是否会记忆并复现训练样本？" |
| 安全 | "拒绝回答的边界是否明确？""如何处理越狱提示？" |
| 环境影响 | "训练/推理的碳足迹是否有估算？" |
| 劳动与经济 | "标注团队的工作条件和报酬是否合理？""目标场景是否可能导致岗位替代？" |

#### 3.2.2 风险热力图

基于问卷得分自动生成六维雷达图 + 热力矩阵：

```
        偏见
         /\
        /  \
  劳动  |    |  误用
       |    |
  环境  |    |  隐私
        \  /
         \/
        安全
```

颜色编码：红(高风险，需强制缓解) / 黄(中风险，建议缓解) / 绿(低风险，记录即可)。

#### 3.2.3 场景生成器 (LLM-assisted)

输入模型描述 → LLM生成3-5个"最坏情况部署场景"（worst-case scenarios），包括：
- 场景描述
- 触发条件
- 潜在危害
- 现有防护有效性评估

人工审核后纳入风险评估报告。

#### 3.2.4 红队测试清单

根据部署场景自动生成结构化的红队测试要求：
- 对抗性提示模板
- 边界条件测试
- 跨文化场景测试
- 长尾/低资源场景测试

---

### 3.3 反思模块 (Reflexivity)

**对应论文 4.2 节**：把价值敏感设计嵌入技术流程。

#### 3.3.1 价值预设引导问卷

开放式问题，引导设计团队暴露隐含假设：

1. "我们假定的'典型用户'是谁？为什么？"
2. "哪些用户或场景不在我们的考虑范围内？"
3. "我们的安全策略优先保护哪些价值？(排序：安全、隐私、自由表达、公平、效率……)"
4. "标注指南中如何定义'有害'？这个定义是否考虑了文化差异？"
5. "哪些价值冲突我们意识到了但尚未解决？"

#### 3.3.2 设计预设声明生成

基于回答自动生成 **设计预设声明 (Design Presumption Statement)**：

> "本系统在设计过程中做出如下价值预设：[列出]。这些预设适用于[场景范围]，在[边界条件]下可能需要重新审视。以下群体[列出]的视角在设计中未被充分代表。以下价值冲突[列出]已被识别但尚未解决。"

#### 3.3.3 盲点分析器 (LLM-assisted)

LLM基于团队的反思回答，自动识别潜在盲点：
- "你们提到了X群体，但Y群体在你们的部署场景中可能同样受影响……"
- "你们的安全策略强调了A，但在B文化语境下，A可能不是最紧迫的风险……"

本质是让LLM扮演一个"故意找茬"的角色，迫使团队面对舒适区之外的议题。

---

### 3.4 包容模块 (Inclusion)

**对应论文 4.3 节**：打开"工程师+标注员"的封闭回路。

#### 3.4.1 利益相关者映射

交互式工具，引导项目组识别并定位受影响群体：

**影响-权力矩阵**（2×2网格，可拖拽放置）：

```
高权力 │ 监管机构        │ 平台方/开发商
      │                 │
      │─────────────────│
      │                 │
低权力 │ 边缘用户群体    │ 受影响的普通公众
      │                 │
      低影响            高影响
```

#### 3.4.2 受影响群体影响权重矩阵

对每个识别出的群体，评估：

| 群体 | 受影响程度 | 受益程度 | 退出难度 | 是否弱势群体 | 参与优先级 |
|------|-----------|---------|---------|------------|-----------|
| 群体A | 高 | 低 | 高 | 是 | 🔴 必须参与 |
| 群体B | 中 | 中 | 中 | 否 | 🟡 建议参与 |
| ... | ... | ... | ... | ... | ... |

#### 3.4.3 参与计划生成器

基于利益相关者映射和论文中的治理建议，自动生成参与计划：

- **机制推荐**：公民陪审团 / 参与式审计 / 开源评估集 / 行业伦理委员会 / 公共意见征询
- **时间安排**：开发阶段 → 上线前 → 部署后3个月 → 年度复审
- **参与范围说明**：哪些决策需要外部参与，哪些属于内部工程判断

---

### 3.5 响应模块 (Responsiveness)

**对应论文 4.4 节**：从一次性对齐到持续治理。

#### 3.5.1 部署后监控仪表盘模板

预设监控维度和指标：

| 维度 | 指标示例 | 告警阈值 |
|------|---------|---------|
| 有害输出 | 用户举报率、自动检测命中率 | 周环比上升50%触发复审 |
| 偏见表现 | 不同群体间的输出质量差异 | 显著差异(p<0.05)触发复审 |
| 滥用检测 | 疑似恶意使用模式 | 实时告警 |
| 拒答率 | 按主题分类的拒绝回答比例 | 过度拒答(>阈值)触发复审 |
| 用户反馈 | NPS、满意度、投诉分类 | 持续下降趋势触发复审 |

#### 3.5.2 复审时间线

自动生成结构化的复审计划：

```
T0 (上线前):  初始伦理评估 → 红队测试 → 缓解措施验证
T+1月:        初期监控数据复审 → 用户反馈分析
T+3月:        综合复审 → 更新风险评估
T+6月:        半年复审 → 考虑模型更新/回滚
T+12月:       年度全面复审 → 重新走完整四维评估
```

每次复审生成独立的复审记录，形成可追溯的治理时间线。

#### 3.5.3 事故响应协议模板

结构化的事故处理流程模板：
1. 事故分级（P0-P3）
2. 上报路径
3. 临时缓解措施
4. 根因分析
5. 公开披露模板
6. 改进措施跟踪

---

## 四、报告系统

### 4.1 输出物

| 输出 | 格式 | 内容 |
|------|------|------|
| 伦理评估报告 | PDF | 完整四维评估结果、风险热力图、利益相关者分析、缓解措施清单、复审时间线 |
| 设计预设声明 | PDF / Markdown | 团队价值预设的公开文档 |
| 复审记录 | JSON / Markdown | 每次复审的时间戳记录，可版本对比 |

### 4.2 报告结构

```
1. 项目概览
2. 风险总览（热力图 + 雷达图）
3. 预测：风险维度详细评估
  3.1 偏见与公平
  3.2 误用风险
  3.3 隐私
  3.4 安全
  3.5 环境与劳动
4. 反思：设计预设声明
5. 包容：利益相关者映射与参与计划
6. 响应：监控计划与复审时间线
7. 缓解措施汇总
8. 签署与审核记录
附录A：红队测试清单
附录B：场景分析详情
```

---

## 五、数据模型

### 5.1 核心实体

```
Project
├── id: UUID
├── name: string
├── model_info: JSON (name, version, base_model, params)
├── deployment_context: JSON (domain, users, languages, interface)
├── team_info: JSON
├── status: enum (draft | in_progress | completed | under_review)
├── created_at: timestamp
├── updated_at: timestamp
└── modules: [ModuleResult]

ModuleResult (per module)
├── module_type: enum (anticipation | reflexivity | inclusion | responsiveness)
├── questionnaire_responses: JSON
├── llm_analysis: JSON (optional)
├── human_review_notes: text
├── score: float (where applicable)
└── completed_at: timestamp

StakeholderMap
├── project_id: UUID (FK)
├── groups: [{name, power, impact, vulnerability, priority}]
├── participation_plan: JSON
└── engagement_log: [{date, method, participants, summary}]

ReviewRecord
├── project_id: UUID (FK)
├── review_type: enum (initial | monthly | quarterly | annual | emergency)
├── date: timestamp
├── findings: text
├── mitigations: text
├── assessor: string
└── status: enum (passed | passed_with_conditions | failed)
```

### 5.2 模板系统

```json
{
  "template_id": "healthcare_llm_v1",
  "domain": "医疗",
  "risk_weights": {
    "privacy": 1.5,
    "safety": 1.5,
    "bias": 1.2,
    "misuse": 1.0,
    "environment": 0.5,
    "labor": 0.8
  },
  "required_modules": ["anticipation", "reflexivity", "inclusion", "responsiveness"],
  "mandatory_questions": ["q_privacy_001", "q_safety_003", "q_bias_002"],
  "regulation_references": ["HIPAA", "医疗器械管理条例", "个人信息保护法"],
  "review_cadence": "quarterly"
}
```

---

## 六、用户工作流

### 6.1 典型流程

```
1. 创建项目 → 填写模型和部署上下文
                    ↓
2. 预测评估 → 完成风险问卷 → 审阅LLM场景 → 生成热力图
                    ↓
3. 反思实践 → 完成价值预设问卷 → 审阅盲点分析 → 生成预设声明
                    ↓
4. 利益相关者映射 → 识别受影响群体 → 制定参与计划
                    ↓
5. 响应规划 → 配置监控指标 → 设定复审时间线 → 准备事故协议
                    ↓
6. 生成报告 → 预览 → 补充人工评注 → 导出PDF → 归档
                    ↓
7. 部署后 → 按时间线触发复审 → 更新评估 → 生成复审记录
```

### 6.2 角色与权限

| 角色 | 权限 |
|------|------|
| 项目负责人 | 创建/编辑项目、分配模块、最终签署 |
| 技术评估者 | 填写技术风险问卷、管理红队测试 |
| 伦理顾问 | 审核反思模块、独立评注 |
| 外部审查者 | 只读访问报告、提交反馈 |
| 管理员 | 模板管理、用户管理、系统配置 |

---

## 七、与论文的映射关系

| 论文章节 | 工具模块 | 转化逻辑 |
|----------|---------|---------|
| 二、价值对齐的技术逻辑与伦理预设 | 项目设置 + 反思模块 | 揭示"谁的价值被纳入"是工具设计的出发点 |
| 三.1 技术困境 | 预测模块 + 场景生成器 | 脆弱性→风险热力图和场景分析 |
| 三.2 规范困境 | 反思模块 + 盲点分析 | "谁的价值"→价值预设声明和盲点检测 |
| 三.3 治理困境 | 包容模块 + 响应模块 | 合法性缺失→参与计划和复审机制 |
| 四、负责任创新框架 | 整体四模块结构 | 理论框架直接映射为评估工作流 |
| 五、结语 | 响应模块的持续治理机制 | "不是一次性完成"→复审时间线 |

---

## 八、开发路线图

### Phase 1: MVP (核心评估流程)
- [ ] 项目设置向导
- [ ] 预测模块：风险问卷 + 热力图可视化
- [ ] 反思模块：价值预设问卷 + 声明生成
- [ ] 基础报告导出 (Markdown → PDF)
- [ ] 单用户模式 + SQLite存储

### Phase 2: 协作与智能辅助
- [ ] 包容模块：利益相关者映射交互界面
- [ ] 响应模块：监控模板 + 复审时间线
- [ ] LLM集成：场景生成 + 盲点分析
- [ ] 报告系统完善 (HTML→PDF, 图表嵌入)

### Phase 3: 部署与生态
- [ ] 多用户协作 + PostgreSQL
- [ ] 模板市场（按领域定制）
- [ ] 开放API
- [ ] 复审历史对比与趋势分析
- [ ] 与CI/CD流水线集成

---

## 九、设计原则

1. **人类主导，AI辅助**：LLM提供建议和分析，最终判断始终由人类做出并记录
2. **可复核性优先**：所有评估结论都附带依据、评估者和时间戳
3. **过程透明**：评估过程本身是可审查的——谁在什么时间依据什么做出什么判断
4. **渐进复杂度**：MVP只要求填写核心内容即可生成报告；高级功能逐步解锁
5. **不替代制度**：工具是制度运行的载体，不是制度的替代品。工具不声称"通过评估=伦理合规"

---

## 十、审阅后的改进与完善方案

现有设计已经能够把"预测-反思-包容-响应"四维框架转化为产品模块，但还需要进一步补强评估方法、证据链、参与闭环和工具自身的伦理边界。否则系统容易退化为"问卷 + 图表 + PDF"，无法真正回应论文中提出的三类问题：技术脆弱性、价值代表性不足和治理合法性缺失。

### 10.1 明确评估对象：从"模型评估"改为"部署项目评估"

当前产品名为 **EthosGate · 善治**。需要在产品中明确：本工具评估的是 **某个模型在特定组织、特定场景、特定用户群体、特定治理条件下的部署项目**，而不是给模型贴一个永久伦理标签。

需要在项目设置向导中补充以下字段：

| 字段 | 说明 | 作用 |
|------|------|------|
| 部署边界 | 模型会被用于哪些任务、不会被用于哪些任务 | 防止评估结论被挪用到其他场景 |
| 决策影响程度 | 输出是建议、半自动决策还是自动决策 | 区分低风险辅助与高风险决策 |
| 人类复核机制 | 哪些输出必须人工确认，复核者是谁 | 连接责任链 |
| 用户退出机制 | 用户是否可以拒用、转人工或申诉 | 衡量受影响者的真实选择空间 |
| 组织责任主体 | 产品、技术、合规、伦理负责人 | 避免"系统负责"的责任空洞 |

报告中应加入一句限定性声明：

> 本报告只适用于上述部署边界内的项目场景。若模型用途、用户群体、数据来源、交互方式或组织责任结构发生重大变化，应重新触发伦理评估。

### 10.2 增加"证据链"设计，避免只靠主观打分

伦理风险不能只由李克特量表得分决定。每个判断都应要求填写或上传证据，使报告可复核。

建议为所有问卷项增加统一的证据字段：

```json
{
  "question_id": "privacy_001",
  "answer": 4,
  "rationale": "训练数据来自用户历史对话，已做脱敏处理，但未完成成员推断风险测试。",
  "evidence_type": "test_report | policy_doc | audit_record | interview_note | legal_review | unknown",
  "evidence_link": "internal://risk/privacy_test_2026_06.pdf",
  "confidence": "high | medium | low",
  "assessor": "张三",
  "reviewer": "李四",
  "timestamp": "2026-06-20T10:00:00+08:00"
}
```

报告中不仅显示分数，还要显示：
- 判断依据是否充分；
- 哪些结论来自测试数据，哪些来自团队自陈；
- 哪些风险目前只有低置信度判断；
- 哪些关键证据缺失。

### 10.3 细化风险评分：区分风险大小与缓解可信度

原设计中的红黄绿热力图直观，但需要解释分数如何产生。建议把每个风险项拆成四个分量：

```text
伦理风险值 = 影响严重性 × 发生可能性 × 暴露规模 × 脆弱性权重
剩余风险值 = 伦理风险值 × (1 - 缓解可信度)
```

| 分量 | 评分范围 | 示例解释 |
|------|----------|----------|
| 影响严重性 | 1-5 | 是否可能造成权利、机会、安全、名誉或经济损害 |
| 发生可能性 | 1-5 | 在真实部署环境中出现的概率 |
| 暴露规模 | 1-5 | 受影响用户数量、频率和场景范围 |
| 脆弱性权重 | 1.0-2.0 | 是否涉及儿童、患者、求职者、低资源语言群体等 |
| 缓解可信度 | 0-1 | 缓解措施是否已测试、可执行、有人负责 |

颜色不应只表示总分，还应暴露原因：
- 高严重性 + 低概率：需要应急预案；
- 中等严重性 + 高暴露规模：需要持续监控；
- 低总分 + 低证据置信度：不能直接判为低风险，应标记为"证据不足"。

### 10.4 加入伦理门禁，而不只是生成报告

如果工具只生成报告，无法形成真正的治理约束。建议增加"门禁规则"：

| 门禁条件 | 系统动作 |
|----------|----------|
| 高风险场景但未完成包容模块 | 禁止生成"可部署"结论，只能导出"待补充评估"报告 |
| 涉及医疗、金融、政务、教育等高影响领域 | 强制填写人工复核、申诉机制和事故响应协议 |
| 存在弱势群体高影响风险 | 强制制定参与计划，并记录未参与原因 |
| 任一核心风险项证据置信度为低 | 报告中显著标注"证据不足"，不得显示绿色通过 |
| LLM生成的场景或盲点未经过人工确认 | 不得纳入正式结论，只能作为附录草案 |

门禁输出建议分为四类：

```text
READY：当前证据支持在限定边界内部署
READY_WITH_CONDITIONS：可有条件部署，但必须完成指定缓解措施
NEEDS_REVIEW：证据不足或参与不足，需复审
BLOCKED：存在未缓解的重大伦理/合规风险，不建议部署
```

### 10.5 强化包容模块：从"识别利益相关者"到"参与闭环"

当前包容模块已有利益相关者映射，但还需要证明参与意见是否影响了决策。否则包容容易变成形式化咨询。

建议新增"参与闭环记录"：

| 字段 | 说明 |
|------|------|
| stakeholder_group | 参与群体 |
| participation_method | 访谈、焦点小组、问卷、公民陪审团、参与式审计等 |
| key_concerns | 主要担忧 |
| design_response | 项目组如何回应 |
| accepted_or_rejected | 是否采纳 |
| rejection_reason | 未采纳时的理由 |
| follow_up_owner | 后续负责人 |

报告中应单列一张"参与意见处置表"，回答三个问题：
1. 哪些受影响群体被纳入了？
2. 他们提出了什么问题？
3. 哪些问题真正改变了设计、部署边界或监控指标？

### 10.6 为 LLM 辅助功能设置边界与审计机制

本项目会使用 LLM 做场景生成、盲点分析和报告叙述，但这部分本身也存在幻觉、偏见和责任转移风险。建议增加"LLM辅助审计层"。

LLM生成内容应保存：

```json
{
  "llm_task": "worst_case_scenario_generation",
  "model": "gpt-4.1 / claude / local-model",
  "prompt_version": "scenario_prompt_v2",
  "input_summary": "仅包含部署场景摘要，不含原始用户数据",
  "output": "...",
  "human_status": "draft | accepted | revised | rejected",
  "human_reviewer": "王五",
  "review_notes": "删除了无法证实的监管推断，补充了教育场景风险。"
}
```

产品界面应明确区分：
- **AI建议**：用于启发，不构成结论；
- **人工确认结论**：可进入正式报告；
- **证据支持结论**：可进入门禁判断。

原则上，LLM不得直接给出"是否合规""是否可上线"的最终判断，只能提供问题、场景、盲点和文本草稿。

### 10.7 补充部署后数据治理与隐私保护

响应模块提出了监控指标，但还需说明监控本身如何避免二次风险。部署后监控可能涉及用户反馈、日志、举报内容和群体属性，这些数据本身具有隐私和再识别风险。

建议新增"监控数据治理"小节：

| 项目 | 要求 |
|------|------|
| 数据最小化 | 只收集复审所需字段，不默认保存完整对话 |
| 脱敏处理 | 报告和可视化不展示原始PII |
| 访问控制 | 原始日志、审计记录、报告草稿分级授权 |
| 保存期限 | 为监控数据设置保留周期和删除规则 |
| 群体属性 | 公平性评估所需敏感属性应采用合规、最小化、可解释方式收集 |
| 用户救济 | 用户举报、申诉和撤回请求应进入复审流程 |

### 10.8 增加法规与规范映射模块

论文引用了《新一代人工智能伦理规范》《关于加强科技伦理治理的意见》《生成式人工智能服务管理暂行办法》和 UNESCO 建议书。产品中应把这些规范转化为可检查项，但要避免声称自动完成法律合规。

建议增加 `ComplianceMapping` 实体：

```text
ComplianceMapping
├── project_id
├── jurisdiction: 中国大陆 / 欧盟 / 美国 / 其他
├── norm_reference: 生成式人工智能服务管理暂行办法 / 个人信息保护法 / UNESCO建议书 / 内部政策
├── requirement_summary
├── related_questions: [question_id]
├── evidence_required
├── status: not_applicable | pending | documented | legal_review_needed
└── reviewer_notes
```

在报告中使用谨慎措辞：

> 本节展示项目与相关伦理规范、监管要求之间的对应关系，用于辅助内部审查和外部沟通，不构成法律意见或合规认证。

### 10.9 完善用户体验：从模块化填写改为"风险驱动工作台"

四个模块不应只是线性表单。建议设计一个总览工作台，让用户始终看到：
- 当前评估进度；
- 未解决的高风险项；
- 证据不足项；
- 需要外部参与的事项；
- 即将到期的复审任务；
- 门禁状态。

核心界面建议：

```text
项目总览
├── 门禁状态：NEEDS_REVIEW
├── 高风险项：3
├── 证据不足项：5
├── 必须参与但未参与群体：2
├── 待完成缓解措施：4
└── 下一次复审：2026-09-20
```

这样用户不是"填完四张表"，而是在处理一组需要闭环的治理任务。

### 10.10 增加产品有效性验证计划

作为课程项目或原型项目，除了实现功能，还应说明如何证明这个工具有用。建议设计小规模评估：

| 验证目标 | 方法 | 指标 |
|----------|------|------|
| 是否提高风险识别质量 | 让两组同学评估同一LLM部署案例，一组使用工具，一组不用 | 识别风险数量、风险覆盖维度、证据完整度 |
| 是否促进价值反思 | 分析使用前后设计预设声明的具体性 | 是否指出被排除群体、价值冲突和边界条件 |
| 是否改善参与意识 | 检查利益相关者映射结果 | 是否识别低权力高影响群体 |
| 是否提升可复核性 | 审查生成报告 | 每个结论是否有依据、责任人和时间戳 |
| 使用负担是否可接受 | 任务完成后问卷/访谈 | 完成时间、主观负担、可理解性 |

MVP阶段可以用 3-5 个案例进行可用性测试，例如：
- 教育辅导LLM；
- 医疗问答LLM；
- 企业招聘筛选助手；
- 政务咨询聊天机器人；
- 面向公众的通用写作助手。

### 10.11 调整开发路线图优先级

建议将"证据链、门禁状态、LLM审计"提前到 MVP，而不是放到后期。否则 MVP 只能展示功能，难以体现伦理治理工具的核心价值。

修订后的 MVP 建议：

- [ ] 项目设置向导：增加部署边界、决策影响、人类复核、退出机制、责任主体
- [ ] 预测模块：风险问卷 + 风险公式 + 证据字段 + 热力图
- [ ] 反思模块：价值预设问卷 + 设计预设声明 + 人工确认流程
- [ ] 伦理门禁：READY / READY_WITH_CONDITIONS / NEEDS_REVIEW / BLOCKED
- [ ] 基础报告导出：包含证据链、风险解释、限制性声明
- [ ] LLM辅助最小闭环：场景生成 + 人工接受/修改/拒绝记录
- [ ] 单用户模式 + SQLite存储

Phase 2 再推进：
- [ ] 利益相关者映射交互界面
- [ ] 参与闭环记录
- [ ] 监控模板与复审时间线
- [ ] 法规与规范映射
- [ ] 报告系统完善

Phase 3 再推进：
- [ ] 多用户协作
- [ ] PostgreSQL
- [ ] 第三方审查者只读入口
- [ ] 复审历史对比与趋势分析
- [ ] CI/CD 或模型发布流程集成

### 10.12 需要避免的设计误区

1. **不要把伦理评估做成总分排名**：伦理风险不是越低分越好，关键是边界、证据、缓解和责任是否清楚。
2. **不要让红黄绿颜色掩盖不确定性**：低证据置信度不能显示为绿色安全。
3. **不要把利益相关者参与做成装饰性模块**：必须记录参与意见如何改变设计。
4. **不要让 LLM 替代伦理判断**：LLM只能辅助发现问题，不能给出最终正当性。
5. **不要声称自动合规**：系统可以辅助合规审查，但不能替代法律、伦理委员会或监管判断。
6. **不要只关注输出内容安全**：还要关注数据来源、劳动条件、组织责任、用户申诉和部署后反馈。

---

## 十一、可落地的下一步实现建议

如果继续推进原型开发，建议优先做一个"高可信 MVP"，而不是追求模块数量完整：

1. **先做项目总览 + 项目设置**：把部署边界、责任主体和人类复核机制固定下来。
2. **再做预测模块**：实现风险问卷、证据字段、风险计算和热力图。
3. **接着做反思模块**：生成设计预设声明，但要求人工确认。
4. **加入门禁状态**：让系统能够输出 `NEEDS_REVIEW` 或 `READY_WITH_CONDITIONS`，体现治理约束。
5. **最后导出报告**：报告必须包含限制性声明、证据链、未解决风险和复审计划。

这样即使暂时不实现复杂协作、多用户和完整法规映射，也能清楚体现论文的核心观点：价值对齐不是一次性技术优化，而是需要预测、反思、包容和响应共同支撑的持续治理过程。
